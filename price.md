## 十四、会员等级制度

## 一、核心机制

- **计费单元**：以 **视频处理时长（秒/分钟）** 为计费单位，而非按视频个数。
- **配额系统**：后台为每位用户维护 `quota_seconds` 字段（剩余可用秒数）。
- **扣费规则**：Whisper API 转录成功后，根据视频原始长度自动扣除相应秒数。
- **超额处理**：当用户 `quota_seconds` < 上传视频时长时，系统拒绝处理，并引导用户「购买增量包」或「升级会员」。

---

## 二、会员等级

### 等级一：免费用户

| 项目 | 说明 |
|------|------|
| **核心权限** | 基础 AI 字幕生成（Whisper 模型），下载 SRT 文件 |
| **初始配额** | 注册即赠送 **300 秒**（5 分钟） |
| **月度配额** | 无，配额为一次性，用完即止 |
| **单任务上限** | **300 秒**（5 分钟） |
| **高级功能** | ❌ 无字幕烧录、样式自定义、高精度识别 |
| **用户支持** | 社区 / FAQ |

> **定位**：零门槛引流工具，让用户体验核心价值，为升级付费创造动机。

---

### 等级二：专业会员（Pro）

| 项目 | 说明 |
|------|------|
| **月度费用** | **¥29 / 月** |
| **月度配额** | 每月 **3000 秒**（50 分钟） |
| **配额结转** | ❌ 当月未用完配额不结转 |
| **单任务上限** | **1800 秒**（30 分钟） |
| **高级功能** | ✅ 高精度识别、在线编辑、字幕烧录、自定义样式 |
| **用户支持** | 邮件支持 |

> **定位**：核心收入来源，面向高频创作者，相当于免费版 10 倍配额 + 解锁全部高级功能。

---

## 三、按量付费包（增量包）

灵活补给，适用于免费用户或配额用尽的专业会员。

| 套餐名称 | 价格 | 可得时长 | 适用场景 |
|----------|------|----------|----------|
| **轻量包** | ¥9.9 | 600 秒（10 分钟） | 应急处理一个短视频 |
| **畅享包** | ¥29.9 | 3000 秒（50 分钟） | 相当于一个月专业会员配额 |
| **专业包** | ¥69.9 | 10000 秒（约 167 分钟） | 高性价比，适合批量处理 |

> **使用机制**：按量包配额**永久有效**。扣费顺序：优先消耗会员月度配额 → 再消耗按量包配额。

---

## 四、定价对比一览表

| 特性 | 免费用户 | 专业会员（¥29/月） |
|------|----------|-------------------|
| 初始 / 月度配额 | 300 秒（一次性） | 3000 秒（每月） |
| 单任务上限 | 5 分钟 | 30 分钟 |
| 高级功能 | ❌ | ✅ |
| 配额类型 | 一次性赠送 | 按月刷新 |
| 月费 | ¥0 | ¥29 |
| 配额单价（元/分钟） | — | ¥0.58 |

---

## 五、技术实现要点（对接现有系统）

1. **数据库字段扩展**（`users` 表）：
   - `quota_seconds`：剩余配额秒数（整数）
   - `total_used_seconds`：累计使用秒数
   - `is_premium`：是否专业会员（布尔）
   - `premium_expire_at`：会员到期时间（时间戳）

2. **扣费逻辑**（`POST /upload` 接口）：
   - 校验 `quota_seconds >= video_duration`
   - 转录成功后执行扣减：`quota_seconds -= video_duration`

3. **购买流程**（`POST /buy-quota` / `POST /upgrade-premium`）：
   - 接收支付回调 → 增加 `quota_seconds` 或设置 `is_premium` + `premium_expire_at`

4. **扣费优先级**：
   - 优先消耗当月会员配额（`quota_seconds` 中标记为 `monthly` 部分）
   - 再消耗按量包配额（`quota_seconds` 中标记为 `topup` 部分）

---

## 六、后续可扩展方向

- 年度会员（¥299/年，送 12 个月配额 + 额外 2 个月）
- 团队版（多用户共享配额池）
- API 按量调用（面向 B 端开发者）